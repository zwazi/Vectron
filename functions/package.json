{
  "name": "vectron-functions",
  "version": "1.0.0",
  "private": true,
  "description": "Trusted account administration for Vectron.",
  "main": "index.js",
  "engines": {
    "node": "22"
  },
  "scripts": {
    "check": "node --check index.js && node --check racing-rating.js && node --check user-audit.js",
    "test": "node --test test/*.test.js"
  },
  "dependencies": {
    "firebase-admin": "^14.2.0",
    "firebase-functions": "^7.3.2"
  }
}
